이 폴더에 vendor/라이선스 원본 추출 CSV를 넣고 `official_index_clone_prepare.py`를 실행합니다.

기본 파일명:

- `review_metadata.csv`
- `universe_export.csv`
- `bucket_targets_export.csv`

실행:

```bash
cd trading-value-worker
python Disclosure/official_index_clone_prepare.py
```
